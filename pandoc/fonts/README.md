Place a legally licensed Monaco font file here as `Monaco.ttf` to use it for
inline code and fenced code blocks in the generated PDF.

Place a Mona Sans regular font file here as `MonaSans-Regular.ttf` to use it
for the main PDF content.

If these files are not present, the PDF build falls back to Linux Libertine for
main text and Ubuntu Mono for code.
